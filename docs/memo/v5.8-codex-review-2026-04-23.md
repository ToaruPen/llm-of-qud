# v5.8 Re-review — Codex (2026-04-23, round 5, CONVERGENCE)

Reviewer: Codex (advisor mode, read-only, reasoning=high)
Target: `docs/architecture-v5.md` v5.8
Cross-reference: `decompiled/` (direct path access)
Previous rounds: v5.4 / v5.5 / v5.6 / v5.7 Codex reviews

---

**1. Go/No-Go 判定: 条件付き Go**

Phase 0-A / 0-A2 着手は Go。
ただし v5.8 を全体 freeze 済み implementation spec として **Clean Go** と呼ぶには、`WAIT_FOR_SUPERVISOR` の supervisor timeout 処理だけ直すべき。

**2. MF-v5.7-1 / MF-v5.7-2 解消状況**

- **MF-v5.7-1 ✅解消**: `state_fallback()` docstring は non-modal `WAIT` と modal `WAIT_FOR_SUPERVISOR` を明確に分離済み (`architecture-v5.md:2019-2023`)。Action enum コメントも modal branch から `WAIT` を返さない方針 (`2139-2143`)。擬似コードも modal branch の terminal failure が `WAIT_FOR_SUPERVISOR` で統一 (`2085, 2095, 2106, 2123`)。
- **MF-v5.7-2 ✅解消**: `supervisor_request.reason` enum、`modal: null` 条件、`diagnostic` required 条件は定義済み (`2150-2156`)。Appendix D level-up 例 (`3317-3338`) は `reason == "level_up"` で `modal` object あり、矛盾なし。

**3. 新規 Must-Fix**

**MF-v5.8-1: supervisor timeout が modal WAIT 禁止ポリシーと衝突**

`Action.WAIT` は non-modal fallback only と明記されているのに (`2139-2143`)、`WAIT_FOR_SUPERVISOR` の timeout が無条件に `Action.WAIT` へ auto-resume する (`2162`, Appendix D `3355`)。level-up / uncancellable modal / modal fallback failure で supervisor が応答しない場合、modal を保持したまま WAIT する経路になり、v5.7 で潰した deadlock 方針と再衝突する。

**4. Should-Fix 残件の最新配置**

- **S1 duplicate guard**: **閉じてよい**。Phase 0-A に繰り込み済み (`2715-2717` reload acceptance)。`RegisterOnBeginPlayerTurnCallback` は実コード `XRLCore.cs:576-579` で単純 `Add`。
- **S2 BackgroundClicked null-safe**: Phase 2a `2-K` / Gate 2 前。`1083` は修正済み。`749`, `774` の軽い表現は 2-K 実装前に `1083` 参照へ寄せる。
- **S3 TextConsole lock/ViewTag**: Phase 0-B。`BufferCS` lock / `ViewTag` 確認は `395-399` で弱い。実コード `TextConsole.cs:29, 149-154`。
- **S6 p95 数学**: Phase 2b。KPI は LLM-owned turns に分離済み (`3051-3055`)、Must ではない。

**5. Clean Go への残件 (v5.9 redline 最小案)**

```diff
-#       6. On supervisor timeout (5 minutes), auto-resume with Action.WAIT
+#       6. On supervisor timeout (5 minutes), apply state-aware timeout policy:
+#          non-modal → Action.WAIT; cancellable modal → cancel_or_back;
+#          level_up / uncancellable modal / modal_fallback_failed / modal_desync
+#          → remain paused and renew supervisor_request. Never return Action.WAIT
+#          while game_state == "modal".
```

Appendix D 同様:
```diff
-`"abort"` (force safe fallback). On supervisor timeout (300s), auto-resume with Action.WAIT.
+`"abort"` (force safe fallback). On supervisor timeout (300s), use the same
+state-aware timeout policy as WAIT_FOR_SUPERVISOR; modal states must not fall
+through to Action.WAIT.
```

L2194 の `modal→default` を `state_fallback()` modal 方針に差し替えるとさらに良い。

**6. 実装着手前の最終読書ガイド**

1. `docs/architecture-v5.md:2701-2773` — Phase 0 ordering、0-A、0-A2 packaging / load criteria
2. `1697-1724` — C# MOD components and responsibilities
3. `1726-1764` — `IPlayerSystem`, `RegisterPlayer`, begin-turn hook, duplicate guard
4. `2388-2435` — wire envelope, `turn_start`, canonical `game_state` / `modal`
5. `3220-3249` — Appendix D `TurnStart` examples and modal schema
6. `386-399` — ScreenBuffer observation basics for 0-B
7. `1765-1784` — thread routing; read before any popup/UI interaction work

**7. 収束判定**

**もう1フルラウンドは不要**。MF-v5.7-1 / 2 は解消済みで、v5.8 は Phase 0-A 着手用 spec として freeze してよい。ただし全体 Clean Go なら、上の supervisor timeout redline だけ v5.9 に入れてから freeze が妥当。
