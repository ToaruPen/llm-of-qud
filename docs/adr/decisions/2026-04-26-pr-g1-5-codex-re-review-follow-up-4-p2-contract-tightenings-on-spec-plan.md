# ADR Decision Record

timestamp: 2026-04-26T11:19:39Z
change: PR-G1.5 codex re-review follow-up: 4 P2 contract tightenings on spec + plan
adr_required: false
rationale: Codex second-pass review on PR-G1.5 (after the first 5-finding tightening) flagged 4 more P2 contract issues. Fixed in-PR. (1) Plan emitted [cmd] with action=PassTurn / dir=null on the 'wait' branch, violating the locked Phase 0-F command_issuance.v1 enum (action ∈ {Move, AttackDirection}, dir non-null, hard-fail otherwise per phase-0-f spec lines 150/175/218). Resolution: drop wait from the locked decision.v1 intent enum AND drop PassTurn from the locked decision.v1 action enum, joining the two schemas at the same action-set lock. HeuristicPolicy's all-blocked fallback now returns explore: Move ExploreOrder[0]; the Move fails, the 3-layer drain emits fallback='pass_turn' on the [cmd] line. command_issuance.v1 stays untouched. Future wait/PassTurn-as-action requires a JOINT decision.v2 + command_issuance.v2 bump. (2) Plan Task 5 Step 1 used HashSet<string> / List<string> in LLMOfQudSystem.cs without instructing the implementer to add 'using System.Collections.Generic;' — Roslyn would fail. Added explicit using-declaration instruction with the current file's import set documented inline. (3) Plan BuildDecisionJson emitted literal '"error":null' instead of serializing decision.Error, hiding policy-returned (non-throwing) error decisions from the acceptance gate. Now serializes via AppendJsonStringOrNull(sb, decision.Error). (4) Plan acceptance validator counted 'jq -r .intent' output of 'null' (from sentinel decision lines that lack the field) as a distinct intent, letting a 1-real-intent + 1-error run falsely pass the >=2 intent-diversity gate. Now filters via 'jq -r select(.intent != null and .intent != "") | .intent'. ADR 0009 file content unchanged in THIS commit's diff (rounds 0+1 already amended Decision #7); ADR 0009 is included in files: list below for push-mode branch-cumulative coverage per scripts/check_adr_decision.py — the gate reads the LATEST decision-log entry's files: list and requires it to cover the union of all push-range trigger-file changes.
files:
  - docs/adr/0009-phase-0-g-rescope-judgment-boundary.md
  - docs/superpowers/plans/2026-04-26-phase-0-g-heuristic-bot.md
  - docs/superpowers/specs/2026-04-26-phase-0-g-heuristic-bot-design.md
adr_paths:
  - docs/adr/0009-phase-0-g-rescope-judgment-boundary.md
